trigger:
- none

stages:
  - stage: Build
    jobs:
      - job: Build
        displayName: Build job
        pool: br-agent

        steps:

        - script: |
            chmod 666 /var/run/docker.sock
            docker inspect --format '{{join .Args " , "}}'
            docker -v

trigger:
- none

stages:
  - stage: Build
    jobs:
      - job: Build
        displayName: Build job
        pool: br-agent

        steps:

        - script: |
            sudo docker inspect --format '{{join .Args " , "}}'
            sudo docker -v

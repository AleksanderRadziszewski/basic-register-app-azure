trigger:
- none

stages:
  - stage: Build
    jobs:
      - job: Build
        displayName: Build job
        pool: br-agent

        steps:

        - script: |
            chmod +x /mnt/vss
            whoami
            whereis docker
          # docker inspect --format '{{join .Args " , "}}'
          # docker -v

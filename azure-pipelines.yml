trigger:
- none

stages:
  - stage: Build
    jobs:
      - job: Build
        displayName: Build job
        pool: br-agent

        steps:

        - script: |
            docker inspect --format '{{join .Args " , "}}'
            docker -v
